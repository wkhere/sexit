prereq
======
% brew install swi-prolog

it needs to be 6.x for apply:foldl


make targets
============

% make          # compiles to exec, entry point: main/1
% make run      # as above plus runs the exec
% make repl     # loads code, runs repl/1 hook and stays in the repl
